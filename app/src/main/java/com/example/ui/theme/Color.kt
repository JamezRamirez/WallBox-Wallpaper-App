package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Immersive Dark Theme Palette (Extracted from UI HTML design)
val CosmicBackground = Color(0xFF0F0F12) // Very dark slate-violet background
val CosmicSurface = Color(0xFF252529) // Mid slate-gray for standard surfaces & chips
val CosmicSurfaceVariant = Color(0xFF1C1B1F) // Deep black-violet background for navigation bar
val ElectricCyan = Color(0xFFD0BCFF) // Accent lavender/purple (Primary)
val ElectricTeal = Color(0xFF381E72) // Primary text or dark purple tint (onPrimary)
val SmoothPink = Color(0xFFEADDFF) // M3 light purple/lavender (Secondary/Tertiary)
val CrispWhite = Color(0xFFE6E1E5) // Main text/foreground off-white
val TextSecondary = Color(0xFFC9C5D0) // Muted lavender-gray for metadata/supporting text

// Light Theme Alternatives (for compatibility)
val PaleBackground = Color(0xFFF8F9FA)
val PaleSurface = Color(0xFFFFFFFF)
val PrimaryBlue = Color(0xFF6750A4)
val NeutralDark = Color(0xFF1C1B1F)

